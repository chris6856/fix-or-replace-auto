{
  "name": "fix-or-replace-auto",
  "private": true,
  "workspaces": [
    "app",
    "backend",
    "packages/*"
  ],
  "scripts": {
    "build:packages": "npm run build --workspace=packages/types && npm run build --workspace=packages/calc-engine",
    "test:calc-engine": "npm run test --workspace=packages/calc-engine",
    "postinstall": "patch-package"
  },
  "devDependencies": {
    "@types/react": "~19.2.2"
  }
}
